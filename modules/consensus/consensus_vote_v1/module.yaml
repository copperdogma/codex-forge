module_id: consensus_vote_v1
stage: consensus
entrypoint: modules/consensus/consensus_vote_v1/main.py
input_schema: portion_hyp_v1
output_schema: locked_portion_v1
default_params:
  min_conf: 0.55
param_schema:
  properties:
    min_conf:
      type: number
      minimum: 0
      maximum: 1
    range_start:
      type: integer
      minimum: 1
    range_end:
      type: integer
      minimum: 1
  required: [min_conf]
notes: "Consensus: vote and greedy selection to pick best non-overlapping portions and fill gaps."
