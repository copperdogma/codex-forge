module_id: build_appdata_v1
stage: export
entrypoint: modules/build/build_appdata_v1/main.py
input_schema: enriched_portion_v1
output_schema: app_data_v1
default_params:
  out: data.json
param_schema:
  properties:
    enriched:
      type: string
    out:
      type: string
    run_id:
      type: string
  required: []
notes: Merge enriched portions into app-facing data.json (graph nodes with choices/combat/items).
