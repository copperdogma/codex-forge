module_id: portionize_detect_sections_v1
stage: portionize
entrypoint: modules/portionize/portionize_detect_sections_v1/main.py
input_schema: clean_page_v1
output_schema: portion_hyp_v1
default_params: {}
param_schema:
  properties:
    pages:
      type: string
    out:
      type: string
  required: [pages, out]
notes: "Rule-based section detector for Fighting Fantasy: finds standalone numeric headers 1-400 and emits portion_hyp_v1 spans."
