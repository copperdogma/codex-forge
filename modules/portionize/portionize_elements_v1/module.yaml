module_id: portionize_elements_v1
stage: portionize
entrypoint: modules/portionize/portionize_elements_v1/main.py
input_schema: unstructured_element_v1
output_schema: portion_hyp_v1
default_params: {}
param_schema:
  properties:
    pages:
      type: string
    out:
      type: string
    state-file:
      type: string
    progress-file:
      type: string
    run-id:
      type: string
  required: []
notes: "Status: experimental. Element-aware portionizer that works directly with Unstructured elements, using element types (Title, NarrativeText) to detect section boundaries. More precise than page-based approaches."

