module_id: portionize_numbered_v1
stage: portionize
entrypoint: modules/portionize/portionize_numbered_v1/main.py
input_schema: clean_page_v1
output_schema: portion_hyp_v1
default_params:
  confidence: 0.6
param_schema:
  properties:
    pages:
      type: string
    out:
      type: string
    confidence:
      type: number
      minimum: 0
      maximum: 1
  required: []
notes: Split clean pages into numbered sections using leading numeric headings; portion_id set to the detected number.
