module_id: section_target_guard_v1
stage: adapter
entrypoint: modules/adapter/section_target_guard_v1/main.py
input_schema: enriched_portion_v1
output_schema: enriched_portion_v1
default_params: {}
param_schema:
  report:
    type: string
    description: Path to write coverage summary JSON.
    required: true
  allow-missing:
    type: boolean
    description: Exit 0 even when some targets are missing.
notes: "Status: experimental. Adapter: map targets to known sections, backfill missing ones as stubs, emit a coverage report, and fail on missing by default."
