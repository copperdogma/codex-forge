module_id: reduce_macro_v1
stage: adapter
entrypoint: modules/adapter/reduce_macro_v1/main.py
input_schema: element_core_v1
output_schema: macro_reduced_v1
description: Reduce elements.jsonl into compact per-page summaries for macro classification (frontmatter/gameplay/endmatter).
default_params:
  max_lines: 10
  max_len: 120
param_schema:
  properties:
    elements:
      type: string
    out:
      type: string
    max_lines:
      type: integer
    max_len:
      type: integer
  required: [elements, out]
