module_id: image_crop_cv_v1
stage: extract
entrypoint: modules/extract/image_crop_cv_v1/main.py
input_schema: page_doc_v1
output_schema: image_crop_v1
default_params:
  min_area_ratio: 0.005
  max_area_ratio: 0.8
  blur: 5
param_schema:
  properties:
    min_area_ratio:
      type: number
      minimum: 0
      maximum: 1
    max_area_ratio:
      type: number
      minimum: 0
      maximum: 1
    blur:
      type: integer
      minimum: 1
  required: [min_area_ratio, max_area_ratio, blur]
notes: "Detect: find illustration regions via OpenCV contours, output crop boxes and cropped image paths."
