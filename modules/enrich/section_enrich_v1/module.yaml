module_id: section_enrich_v1
stage: enrich
entrypoint: modules/enrich/section_enrich_v1/main.py
input_schema: portion_hyp_v1
output_schema: enriched_portion_v1
default_params:
  max_chars: 2000
param_schema:
  properties:
    pages:
      type: string
    portions:
      type: string
    out:
      type: string
    max_chars:
      type: integer
  required: []
notes: "Heuristic section enricher: adds section_id (numeric heading) and targets (turn-to numbers) without LLM calls."
