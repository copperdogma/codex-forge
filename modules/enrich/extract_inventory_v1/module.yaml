module_id: extract_inventory_v1
stage: enrich
description: "Extract inventory actions (gaining, losing, using, checking items) from section text."
entrypoint: modules/enrich/extract_inventory_v1/main.py
input_schema: enriched_portion_v1
output_schema: enriched_portion_v1
default_params:
  model: "gpt-4.1-mini"
  use_ai: true
  max_ai_calls: 50
param_schema:
  properties:
    model:
      type: string
      description: "LLM model for complex extraction (default: gpt-4.1-mini)"
    use_ai:
      type: boolean
      description: "Use LLM for complex extraction cases (default: true)"
    max_ai_calls:
      type: integer
      description: "Maximum LLM calls for this run (default: 50)"
  required: []
notes: "Hybrid extraction using regex for common inventory phrases and LLM for complex/ambiguous cases."
