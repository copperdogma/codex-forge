module_id: assert_section_targets_v1
stage: adapter
entrypoint: modules/validate/assert_section_targets_v1.py
input_schema: enriched_portion_v1
output_schema: null
default_params:
  allow-missing: false
param_schema:
  properties:
    allow-missing:
      type: boolean
  required: []
notes: "Validate: ensure every target exists as a section_id or portion_id; fail the run unless allow-missing is set."
