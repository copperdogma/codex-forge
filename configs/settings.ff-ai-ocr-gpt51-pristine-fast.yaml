# GPT-5.1 AI OCR pipeline with FAST extraction for pristine PDF
# Story 084: Uses extract_pdf_images_fast_v1 for 974× speedup
# Pristine PDF has 72 DPI embedded images - OCR quality validated at native resolution

input:
  pdf: input/deathtrapdungeon00ian_jn9_1 - from internet archive.pdf

stage_params:
  # Override extraction module to use fast extraction
  extract_pdf_images_capped:
    module: extract_pdf_images_fast_v1
    params:
      start: 1
      end: 228  # pristine PDF has 228 pages (1-up layout)
      fallback_to_render: true
      fallback_dpi: 300
