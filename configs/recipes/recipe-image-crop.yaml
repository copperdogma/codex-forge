stages:
  - id: extract
    modules:
      - module_id: image_crop_cv_v1
        needs: []
        params:
          min_area_ratio: 0.005
          max_area_ratio: 0.99
          blur: 3
          topk: 5
        inputs:
          pages: output/runs/RECENT/pages.jsonl
        outputs:
          out: output/runs/RECENT/image_crops.jsonl
          crop-dir: output/runs/RECENT/crops/
