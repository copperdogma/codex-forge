# Story-070: Full book validation for gutter detection
# Run pages 1-113 but stop after OCR to avoid wasting time on AI processing

input:
  pdf: input/06 deathtrap dungeon.pdf

stage_params:
  intake:
    start: 1
    end: 113
    engines: ["tesseract", "apple"]  # Use fast engines only
    write_engine_dumps: true
  easyocr_guard:
    allow_missing: true
    min_coverage: 0.0
