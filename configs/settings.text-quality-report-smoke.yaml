models:
  default: gpt-4.1-mini

stage_params:
  report:
    # Override this to point at any JSONL artifact you want to analyze.
    # Examples:
    #   output/runs/<run_id>/pagelines_final.jsonl
    #   output/runs/<run_id>/elements_core_typed.jsonl
    in: ""
    report: text_quality_report.json
