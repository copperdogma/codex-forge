# GPT-5.1 AI OCR pipeline with FAST extraction for old PDF
# Story 084: Uses extract_pdf_images_fast_v1 for 28× speedup
# Old PDF has 150 DPI embedded images - fast extraction proven identical to rendering

input:
  pdf: input/06 deathtrap dungeon.pdf

stage_params:
  # Override extraction module to use fast extraction
  extract_pdf_images_capped:
    module: extract_pdf_images_fast_v1
    params:
      start: 1
      end: 113  # full book
      fallback_to_render: true
      fallback_dpi: 300
