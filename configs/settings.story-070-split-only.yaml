# Story-070: Split-only test (no OCR)
# Just does page splitting to test center-biased algorithm

input:
  pdf: input/06 deathtrap dungeon.pdf

stage_params:
  intake:
    start: 1
    end: 113
    split_only: true  # Skip OCR, just do splitting
    engines: ["pdftext"]  # Not used in split-only mode, but required by schema
    write_engine_dumps: false

