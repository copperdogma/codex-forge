# Story-070: Test per-page gutter detection on problem pages
# Test pages 21-26 to verify the fix for bad splits

input:
  pdf: input/06 deathtrap dungeon.pdf

stage_params:
  intake:
    start: 21
    end: 26
    engines: ["tesseract", "apple"]  # Use only fast engines for testing
    write_engine_dumps: true
  easyocr_guard:
    allow_missing: true  # We're not using easyocr for this test
